1) What is the difference between null and undefined?
   Ans: Undefined is when variable is decleared but there is no value. Null is assigned by us that the value is Null means no value.
2) What is the use of the map() function in JavaScript? How is it different from forEach()?
   Ans: map() jump to every element of an array and return a new array. But forEach() does not return any new array.
3) What is the difference between == and ===?
   Ans: Double equal check the value only. Triple equal checl value and datatype equal or not.
4) What is the significance of async/await in fetching API data?
   Ans: In javaScript async help to jump in next line if it takes time. And await make stop the works untill provide promises. It make API handling better.
5) Explain the concept of Scope in JavaScript (Global, Function, Block).
   Ans: scope is where to decleared variables. Global scope decleared outside the function. Function scope use inside function and can not access from outside of function. Block scope decleared inside the block({}) only.
